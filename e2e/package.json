{
  "name": "trailersys-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Suite de automatizacion E2E (Playwright) del login de TrailerSys",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
